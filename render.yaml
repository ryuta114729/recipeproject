databases:
  - name: mysitedb
    plan: free
    databasename: recipedata
    user: oreryuta46

services:
  - type: web
    plan: free
    name: recipebook
    runtime: python
    buildCommand: "./build.sh"
    startCommnd: "python -m gunicorn recipeproject.asgi:applicaton -k uvicorn.workers.UvicornWorker"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysitedb
          property: connectionString
      - key: WEB_CONCUREENCY
        value: 4